---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json
type: containerfile
snippets:
  - |
    # Enable VMware systemd services
    RUN systemctl enable vmware-networks.service vmware-usbarbitrator.service vmware-networks.path vmware-usbarbitrator.path
